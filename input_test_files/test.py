import ast as a
import time as t
from math import sin as si, cos as co
import torch.nn as tf


t.SGD(t.SparseAdam(y = t.SGD(t.SparseAdam(z = [q.index(v) if v in q else 99999 for v in vm], x = '3', y = 3))))
tf.Conv2d(in_channels = True, out_channels = 4)
test(y = 3).lol.test().test(x = 3)
